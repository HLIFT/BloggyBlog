<h5 class="pb-3 text-center">Les derniers commentaires</h5>
<div class="text-center">
    <div class="list-group">
        {% for comment in comments %}
            <a href="{{ path('comment.show', {idComment: comment.id}) }}" class="list-group-item list-group-item-action">
            {{ comment.username }}
            </a>
        {% endfor %}
    </div>
</div>
<h5 class="text-center pt-5 pb-3">Catégories</h5>
<div class="text-center">
    <div class="list-group">
        {% for category in categories %}
            {% for post in category.posts %}
                


            {% endfor %}
            {% if category.posts != null %}
                <a href="{{ path('category.show', {id: category.id}) }}" class="list-group-item list-group-item-action">
                {{ category.name }}
                </a>
            {% endif %}
        {% endfor %}
    </div>
</div>
